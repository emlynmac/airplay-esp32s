.cache/
.claude/
.pio/
.venv/
.venv-zensical/
.vscode/
# Anchored to the repo root — an unanchored "build*" also swallows files like
# docs/reference/build-environments.md and .github/workflows/build.yml.
/build*

# Documentation site output
/site/

# Local tooling artifacts (screenshots, browser traces)
/output/
managed_components/
# Generated by ESP-IDF / PlatformIO. Anchored to the root so they cannot shadow
# the tracked board defaults in config/.
/sdkconfig
/sdkconfig.*

# Local per-board overrides — see docs/boards/custom.md
config/sdkconfig.user.*

#
user_platformio.ini

# macOS metadata
.DS_Store
**/.DS_Store

# Host-side hybrid flow harnesses (compiled locally)
components/dac_tas57xx/hybridflows/tools/hf1read
components/dac_tas57xx/hybridflows/tools/hf3read
components/dac_tas57xx/hybridflows/tools/hf3test
components/dac_tas57xx/hybridflows/tools/pbetest
components/dac_tas57xx/hybridflows/tools/__pycache__/

# 8.8 MB reference doc — download from ti.com rather than tracking it
components/dac_tas58xx/TAS58xx Tuning Guide.pdf

# 106 KB of SPIFFS for a cosmetic image, on boards that mostly have no display
# at all — it does not fit alongside the web UI in a 4 MB layout. Generate one
# with components/display/make_background.py; the driver renders a blank
# background without it. See docs/features/tft-display.md.
data/bg/
